# Sample asks

A STRANGER OPENS IT — paste a failing setup and ask for a scored audit. Sub-bullets are notes — not the paste.

They enter (authored): A stranger describes the failing setup they rely on — what it is supposed to do, who gets hurt when it fails, and a few real failing inputs. The tool walks five checks conversationally, proposes findings with the measurement that would confirm each, and returns a scored audit with a severity story, a call, and a tripwire. Instantiate every example from the builder's specimen — never leave a pack demo domain in the shipped files.

1. My failing setup: Shoppers ask about refunds, but the FAQ bot answers with shipping times because it latched onto the product name. Fix that before the busy sale week. Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Persona: Ecommerce CX manager
   - Situation: FAQ bot answers the wrong question
   - Expect: Scored per-check findings with measurements, a severity story, a call, and a tripwire — no coach questions.

2. My failing setup: how long do i have to return the Nova Buds after they ship Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Persona: Ecommerce CX manager
   - Situation: FAQ bot answers the wrong question
   - Expect: Scored per-check findings with measurements, a severity story, a call, and a tripwire — no coach questions.

3. My failing setup: Nova Buds delivery says Friday — can i still cancel Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Persona: Ecommerce CX manager
   - Situation: FAQ bot answers the wrong question
   - Expect: Scored per-check findings with measurements, a severity story, a call, and a tripwire — no coach questions.

4. My failing setup: refund for wrong size on the Trail Jacket, not a shipping question Run the five checks. For each check give a finding plus the measurement that would confirm it. End with severity, call, and tripwire.
   - Role: stranger
   - Persona: Ecommerce CX manager
   - Situation: FAQ bot answers the wrong question
   - Expect: Scored per-check findings with measurements, a severity story, a call, and a tripwire — no coach questions.

## How to score a try

- stranger: shell product audits their failing setup (not coaching them).
- Expect scored checks + severity + call + tripwire — never a coach question.
